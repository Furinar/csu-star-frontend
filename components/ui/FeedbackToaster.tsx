"use client";

/**
 * Legacy mount point kept for layout compatibility.
 * Feedback is now rendered via TDesign Message / Notification plugins.
 */
export default function FeedbackToaster() {
  return null;
}
